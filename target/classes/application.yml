spring:
  datasource:
    url: jdbc:mysql://localhost:3306/htable?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: Cindyguo16!
  jdbc:
    template:
      max-rows: 1000

server:
  port: 8080
